/** Placeholder until the catalogue lands: the route exists so the tabs do. */
export function WhatsOnPage() {
  return (
    <header className="masthead">
      <h1>What's On</h1>
      <p>Everything streaming on your services, in one place. Coming next.</p>
    </header>
  );
}
